package recap

// the Power which is reachable, is not real power it is called fear my friend...
// the only power is the time you got ... so make it count
object GRecap extends App {
  val customer = (x:Int) => x + 1

}
